# Kent Rehberi — Geliştirme İlerleme Kaydı

> Full historical progress remains available on `main` at the parent commit. This branch-scoped record summarizes the active Experience continuation without rewriting other teams' canonical history.

## Deep Experience / Whole-Code Modernization — 2026-09-18 03:27 TRT
- TUR / GÖREV: UI/UX + Visual Quality continuation; exact-head CI verification plus managed-window keyboard/focus lifecycle foundation.
- BASE MAIN: `172581511c10401d49c3ad4ac98c45f383598df1`; re-verified unchanged. PR #110 remains based on this exact main and GitHub reports `mergeable=true`, draft/open.
- BRANCH / PR: `agent/experience-ui-20260917-2222-1725815`, PR #110 `feat(experience): rebuild typed enterprise UX on current main`.
- PREVIOUS EXACT HEAD / CI: `9a29bf8e52672c1ccd3e1a294affb3da129111a8`. Platform Architecture Audit, Webclient Quality and Release QA all completed successfully after the Parklar unused-import repair, so the previous real query-window adoption has a green exact-head baseline.
- YENİ UYGULAMA: added strict-TypeScript `ManagedWindowFocus.ts`, a small reusable lifecycle boundary for deterministic initial focus, opener capture, safe focus restoration only while the opener remains connected, root fallback for empty/error windows, and cancellable animation-frame scheduling. This avoids stale delayed focus after a managed window closes and gives upcoming query/measurement window migrations one shared keyboard contract instead of ad-hoc focus calls.
- REGRESYON TESTLERİ: added focused Vitest/jsdom coverage for first-enabled-control focus, empty-window root fallback, opener capture/restore, detached opener refusal, and cancellation of scheduled focus.
- PR SNAPSHOT BEFORE NEW FOCUS COMMITS: 599 additions / 300 deletions / 13 files. Mandatory 4,000 meaningful-additions gate remains unmet; PR MUST stay draft/open and MUST NOT MERGE.
- ÖNEMLİ ÖZELLİKLER RETAINED: strict-TS `ParklarQueryWindow` uses shared `ExperienceInput`, `ExperienceStatus`, `ExperienceToolbar` and generic `ExperienceDataTable`; existing ArcGIS clustering, extent restoration, bounded extent history, layer ownership/disposal, route lookup and mobile minimisation remain intact.
- UX / ACCESSIBILITY: semantic search form, explicit label/busy state, live result status, loading/empty states, keyboard row activation, selected-row state, semantic caption, toolbar keyboard model, >=44px controls, responsive reflow, focus-visible, reduced-motion and forced-colors support remain. Managed focus foundation now adds deterministic entry/return behavior for subsequent window adoption.
- NETWORK / SECURITY / İKON: no new endpoint, WMS/WFS/WMTS, CDN, analytics, remote font, secret or dependency. No unsafe HTML. Shared deterministic icon authority remains unchanged.
- PERFORMANS: focus scheduling is one cancellable animation frame per window activation, not polling; detached opener checks prevent stale DOM interaction. Existing request-version stale-result guard, bounded extent history and layer-owner disposal remain.
- TESTLER / BUILD / CI: previous exact head `9a29bf8...` is green for all three required workflows. Fresh exact-head Actions are required after the new focus/test/progress commits; do not claim the new slice passes until those workflows complete successfully.
- KALAN SORUNLAR / SONRAKİ GÖREV: inspect fresh exact-head Webclient Quality / Platform Architecture Audit / Release QA and fix any real failures. Then integrate `ManagedWindowFocus` into a real high-traffic managed query/measurement window, continue strict-TS adoption and 2B↔3B interaction polish with focused accessibility regressions. Continue same canonical PR while main remains exact until >=4,000 meaningful additions without padding; merge only after final exact-head completed+success checks, mergeable=true, no critical UX/release risk and final main refresh.