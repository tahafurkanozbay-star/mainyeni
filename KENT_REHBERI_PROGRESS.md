# Kent Rehberi — Geliştirme İlerleme Kaydı

> Full historical progress remains available on `main` at the parent commit. This branch-scoped continuation record intentionally summarizes the current Platform turn while preserving the canonical parent history.

## Deep Platform / Architecture — 2026-09-18 02:00 TRT
- TUR / GÖREV: Deep Platform / Whole-Code Modernization continuation; green exact-head verification followed by adaptive runtime pressure/capacity control.
- BASE MAIN: `172581511c10401d49c3ad4ac98c45f383598df1`.
- BRANCH / PR: `agent/platform-adaptive-runtime-20260917-2301-1725815`, PR #112 `feat(platform): rebuild adaptive runtime control plane on current main`.
- PREVIOUS VERIFIED HEAD: `94a7886fb19930d6136a93292c419dbf38ae6884`; Platform Architecture Audit, Webclient Quality and Release QA all completed+success for that exact head. PR was mergeable=true but only 315 additions, so it correctly remained draft/unmerged.
- NEW WORK: added strict-TypeScript `pressureController.ts` and runtime barrel export. The controller combines bounded queue depth, p95 latency, failure rate, frame pacing and existing resource-budget pressure into a normalized score; applies nominal/elevated/high/critical states; uses recovery hysteresis to avoid capacity oscillation; and derives constrained network/CPU/queue/2D/3D/GPU/background budgets from the existing RuntimeBudget contract instead of creating a competing budget model.
- BOUNDS / LIFECYCLE: decision history is capped, recovery requires consecutive low-pressure samples, capacity floors prevent zero-progress starvation, and reset is deterministic. No polling/background timer is introduced; callers explicitly feed observations.
- NETWORK / SECURITY: no endpoint, WMS/WFS/WMTS, CDN, analytics, remote telemetry transport, direct browser data fetch, secret or token was added. Existing same-origin and bounded runtime policies remain authoritative.
- PERFORMANCE: adaptive budgets reduce expensive work admission during sustained queue/latency/failure/frame/resource pressure while preserving nominal capacity under healthy conditions. Hysteresis reduces thrashing during transient recovery.
- MODERNIZASYON: retain React 19 + Vite 8 + TypeScript 7 and compose with existing resourceBudget/runtime supervision. Deprecated FontAwesome/esri-loader/crypto-js cleanup remains a staged compatibility backlog rather than a blind package bump.
- MERGE DURUMU: NOT MERGED. The new head requires a fresh exact-head CI cycle and the PR remains far below the mandatory 4,000 meaningful-additions gate.
- SONRAKİ GÖREV: inspect exact-head CI for the pressure controller, fix any real strict-lint/typecheck/Vitest/build/release failure, then add focused pressure-controller regressions and integrate the controller with admission/resource lifecycle without polling. Continue meaningful Platform work on this same canonical PR while current main/merge-base remains exact; if it becomes behind/diverged, follow branch lifecycle rules. Merge only after >=4,000 additions, all exact-head required checks completed+success, mergeable=true, security/performance/regression review and final current-main refresh.

## Deep Platform / Architecture — 2026-09-18 03:00 TRT
- TUR / GÖREV: adaptive runtime control-plane regression hardening after exact-head green verification.
- BASE / LIFECYCLE: current `main` remains `172581511c10401d49c3ad4ac98c45f383598df1`, identical to PR #112 base SHA; PR is open, draft and mergeable=true, so same canonical branch remains valid.
- VERIFIED HEAD: `ffca343e38906495249f143165973037fa09ff59` completed Platform Architecture Audit, Webclient Quality and Release QA successfully. This verifies the pressure-controller implementation itself on exact head.
- NEW WORK: added focused Vitest suites for `pressureController` and `admissionController`. Pressure coverage exercises healthy/critical classification, reason attribution, capacity floors, malformed/zero denominators, custom weights, bounded history, reset and multi-stage hysteresis recovery. Admission coverage exercises global/lane concurrency and queue bounds, weighted cost, deterministic priority/FIFO ordering, lane head-of-line avoidance, shedding, AbortSignal cancellation/reason propagation, queue expiry, selective cancellation, snapshots and deterministic disposal.
- DEĞİŞEN DOSYALAR: `Webclient.app/src/platform/runtime/pressureController.test.ts`, `Webclient.app/src/platform/runtime/admissionController.test.ts`, this progress record.
- TEST / BUILD / CI: test commits are `7a9e6a429a1ffd7d315ccae5ad8507ad73899818` and `6b954a67474ed45812fb453c57476c6440c0b07e`. Exact-head Platform Architecture Audit is queued and Webclient Quality / Release QA are pending for `6b954a6...`; therefore no PASS is claimed for the newly added regression suites yet.
- NETWORK / SECURITY: no network behavior, endpoint, WMS/WFS/WMTS, secret, token, analytics, telemetry transport or polling added. Tests specifically protect bounded queues/history and cancellation/disposal semantics.
- PERFORMANCE / REGRESSION: tests lock in capacity shedding, cost accounting, lane isolation and hysteresis behavior to prevent future unbounded work amplification or oscillating runtime budgets.
- MERGE DURUMU: NOT MERGED. PR remains below the mandatory 4,000 meaningful-additions gate and the newest exact-head CI is not completed.
- SONRAKİ GÖREV: inspect exact-head CI diagnostics, fix any real lint/typecheck/Vitest/build/release failure, then continue same canonical PR with meaningful admission/pressure integration into runtime supervision and lifecycle. Recheck current main/merge-base before adding further work; merge only after >=4,000 additions plus completed+success exact-head checks, mergeable=true and final security/performance/regression review.

## Deep Platform / Architecture — 2026-09-18 04:00 TRT
- TUR / GÖREV: admission + pressure integration into a composable adaptive runtime control plane.
- BASE / LIFECYCLE: current `main` remains `172581511c10401d49c3ad4ac98c45f383598df1`, equal to PR #112 base; PR remained open/draft/mergeable=true before this implementation pass.
- VERIFIED HEAD: `64f9d4da343f4e307ab0ee81611fa6ff3970af89` completed Platform Architecture Audit, Webclient Quality and Release QA successfully, verifying the prior admission/pressure regression suites on exact head.
- NEW WORK: added `adaptiveRuntimeControl.ts`, a strict-TypeScript composition boundary that derives admission capacity from the existing RuntimeBudget, exposes bounded acquire/cancel/snapshot lifecycle, feeds live admission queue depth together with caller-supplied latency/failure/frame/resource observations into the pressure controller, and returns one effective constrained RuntimeBudget without creating a second budget authority. Added the runtime barrel exports and focused integration regressions.
- REGRESSION COVERAGE: tests cover budget-derived admission, live queued-work pressure, critical budget contraction, selective background cancellation while active foreground work survives, and deterministic post-disposal rejection.
- NETWORK / SECURITY: no endpoint, WMS/WFS/WMTS, direct fetch, remote telemetry, analytics, secret/token, polling or background timer added. Observation remains explicit/in-process; bounded queue/cancellation/disposal contracts remain authoritative.
- PERFORMANCE: integration makes queue pressure actionable alongside latency/failure/frame/resource pressure and provides a single effective budget for downstream consumers; it does not yet mutate the existing kernel scheduler automatically, avoiding hidden feedback loops until exact-head validation and lifecycle integration are proven.
- COMMITS: implementation `3b08fe36742fd028507b7fd7b7b8a208fa09dcc0`, barrel `db6b68b00a2f92a648f47800749a94e0941fc307`, integration tests `9f5600e74144ca505dbf5f8966c820efe355232f` before this progress commit.
- TEST / BUILD / CI: no workflow run was yet associated with exact implementation-test head `9f5600e...` at the immediate check, so no PASS is claimed for this new slice. Next turn must inspect exact-head strict lint/typecheck/Vitest/build/release results and fix real diagnostics on the same canonical PR.
- MERGE DURUMU: NOT MERGED. PR was 1,039 additions before this slice and remains below the mandatory 4,000 meaningful-additions threshold; exact new-head CI is also not completed.
- SONRAKİ GÖREV: verify this exact-head integration through Platform Architecture Audit, Webclient Quality and Release QA; fix any real diagnostics; then continue meaningful lifecycle integration so effective pressure budgets can safely update scheduler/resource admission with explicit hysteresis and rollback semantics. Preserve no-polling/no-new-network behavior and require >=4,000 additions plus final exact-head green CI/mergeability/current-main refresh before merge.
