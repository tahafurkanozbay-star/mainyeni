# Kent Rehberi — Geliştirme İlerleme Kaydı

> Full historical progress remains available on `main` at the parent commit. This branch-scoped record summarizes the active Experience continuation without rewriting other teams' canonical history.

## Deep Experience / Whole-Code Modernization — 2026-09-18 05:26 TRT
- TUR / GÖREV: UI/UX + Visual Quality continuation; exact-head Release QA diagnosis, security-safe focus regression repair, and managed-window focus lifecycle hardening.
- BASE MAIN: `172581511c10401d49c3ad4ac98c45f383598df1`; re-verified unchanged. Branch compare at code head `d4a28b6f94a5faeeec240a2bf4cc3dd68979f219` is 27 commits ahead / 0 behind and merge-base is exact current main. PR #110 is open/draft and GitHub reports mergeable=true before this progress commit.
- BRANCH / PR: `agent/experience-ui-20260917-2222-1725815`, PR #110 `feat(experience): rebuild typed enterprise UX on current main`.
- EXACT-HEAD CI DIAGNOSIS: head `a3cda72c9bc0c828581e791cfd6eb8902a0db236` completed Platform Architecture Audit=success and Webclient Quality=success. Release QA failed only in `typed-release-audit` exact-base regression gate; backend release validation and webclient release validation both succeeded. Webclient release validation passed dependency/lockfile, production dependency audit, Vite contract, full + strict changed-source lint, TypeScript diagnostics, exact-base TypeScript regression, full Vitest, exact-base Vitest regression, native tooling tests, production build/integrity and build budgets.
- RELEASE QA ROOT CAUSE / REPAIR: downloaded the exact-head `typed-release-qa` artifact and verified the new regression findings were four `security|unsafe-html-sink` findings in `ManagedWindowFocus.test.ts` caused by test fixture assignment through `document.body.innerHTML`. Test setup now constructs fixtures with DOM APIs and resets with `replaceChildren()`, removing the unsafe sink instead of weakening the release gate or adding debt allowlisting.
- MANAGED WINDOW LIFECYCLE: `ManagedWindowFocus.ts` now exposes `createManagedWindowFocusLifecycle()`. It captures the opener once per open cycle, schedules deterministic first-control focus, cancels stale animation-frame focus on repeated open/close/dispose, restores focus only to a still-connected opener, clears retained opener references, and remains decoupled from the legacy WindowManager implementation.
- REGRESSION COVERAGE: focused jsdom/Vitest tests cover first enabled control, root fallback, opener capture/restore, detached opener rejection, scheduled-focus cancellation, lifecycle open/focus/close, repeated open notifications preserving the original opener, close-before-frame cancellation, detached opener fail-closed behavior and dispose cleanup. Test fixtures contain no HTML-string sink.
- PR SNAPSHOT AT CODE HEAD: base...head = 888 additions / 300 deletions / 15 files. Mandatory 4,000 meaningful-additions gate remains unmet; PR MUST stay draft/open and MUST NOT MERGE.
- ÖNEMLİ ÖZELLİKLER RETAINED: strict-TS `ParklarQueryWindow` uses shared `ExperienceInput`, `ExperienceStatus`, `ExperienceToolbar` and generic `ExperienceDataTable`; existing ArcGIS clustering, extent restoration, bounded extent history, layer ownership/disposal, route lookup and mobile minimisation remain intact.
- UX / ACCESSIBILITY: semantic search form, explicit label/busy state, live result status, loading/empty states, keyboard row activation, selected-row state, semantic caption, toolbar keyboard model, >=44px controls, responsive reflow, focus-visible, reduced-motion and forced-colors support remain. The shared focus lifecycle now has a stateful controller suitable for safe adoption by real managed windows.
- NETWORK / SECURITY / İKON: no new endpoint, WMS/WFS/WMTS, CDN, analytics, remote font, secret or dependency. The Release QA unsafe-HTML regression was removed at source. Shared deterministic icon authority remains unchanged.
- PERFORMANS: no new polling/network work. Focus scheduling is one animation frame per open notification and is explicitly cancelled on replacement/close/dispose. Existing request-version stale-result guard, bounded extent history and layer-owner disposal remain.
- TESTLER / BUILD / CI: exact head `a3cda72...` proves the webclient lint/typecheck/Vitest/build path green, but overall Release QA was red because of the unsafe test sinks. Repairs and lifecycle expansion changed the head, so fresh exact-head Platform Architecture Audit / Webclient Quality / Release QA are mandatory before PASS can be claimed for the new code.
- KALAN SORUNLAR / SONRAKİ GÖREV: inspect fresh exact-head workflows; fix only real failures. If green, adopt `createManagedWindowFocusLifecycle()` in a real high-traffic managed query/measurement window and add integration-level focus return regressions, then continue second query-screen strict-TS adoption and 2B↔3B interaction polish. Continue same canonical PR while main remains exact until >=4,000 meaningful additions without padding; merge only after final exact-head completed+success checks, mergeable=true, no critical UX/release risk and final main refresh.
