# Kent Rehberi — Geliştirme İlerleme Kaydı

> Full historical progress remains available on `main` at the parent commit. This branch-scoped record summarizes the active Experience continuation without rewriting other teams' canonical history.

## Deep Experience / Whole-Code Modernization — active PR #110 summary
- BASE / BRANCH: exact `main` remains `172581511c10401d49c3ad4ac98c45f383598df1`; branch `agent/experience-ui-20260917-2222-1725815` remains the canonical Experience continuation and is 36 commits ahead / 0 behind with merge-base equal to current main.
- RETAINED WORK: strict-TypeScript Experience form/status/table/query/pagination/toolbar primitives; responsive/reduced-motion/forced-colors styling; strict-TS `ParklarQueryWindow`; shared `ManagedWindowFocus` lifecycle with opener ownership, first-control focus, stale-frame cancellation, hidden/inert/ARIA-disabled filtering, detached-opener fail-closed behavior and non-modal focus-return policy.
- RETAINED ACCESSIBILITY / UX: semantic forms/tables/toolbars, keyboard row activation and toolbar navigation, live/busy/empty states, visible focus, >=44px controls, responsive table reflow and deterministic focus lifecycle.
- NETWORK / SECURITY / ICON: no WMS/WFS/WMTS UI, endpoint, CDN, analytics, remote font, secret or dependency introduced by this Experience PR. Shared deterministic icon authority remains unchanged. Previous unsafe HTML test fixture sink was removed at source.
- PREVIOUS CI: Platform Architecture Audit has remained green across the recent focus slices. Webclient Quality and Release QA on head `10285ad9fc1b94ecbe0e1c3a1fbf49d4ba86502d` failed only because the exact-base Vitest regression gate detected one new Experience test failure; strict changed-source lint and exact-base TypeScript regression passed.

## Deep Experience / Whole-Code Modernization — 2026-09-18 08:26 TRT
- TUR / GÖREV: exact-head Vitest diagnosis and repair, followed by a second real legacy UI modernization slice.
- CI ROOT CAUSE: Webclient Quality run `35306832151` identified exactly one new failure versus base: `ManagedWindowFocus.test.ts :: managed window focus lifecycle cancels stale scheduled focus when the window closes before the frame`. The implementation correctly applies the non-modal `if-focus-within` policy: when focus never entered the window, `close()` cancels the pending frame but does not perform or report a redundant opener restore. The test incorrectly expected `true` despite focus already remaining on the opener.
- FIX: regression expectation now asserts `close() === false`, while still verifying pending animation-frame work is cancelled and active focus remains on the opener. No production behavior or release gate was weakened.
- SECOND REAL-SCREEN SLICE: `FastAccessQuery/ABBQuery/ABBQueryWindow.js` was replaced by strict `ABBQueryWindow.tsx`. The old launcher carried many unused query/GIS/Bootstrap/icon imports and rendered a clickable `<div>` containing an unlabeled image. The replacement is intentionally dependency-light, uses a native `<button type="button">`, exposes an explicit screen-reader label, marks the decorative icon appropriately, and returns `null` after delegating to `WindowManager.ShowWindow('halkekmek-query-window')` instead of leaving a hidden interactive subtree.
- PERFORMANCE / MODERNIZATION: removing unused query/business/GIS/Bootstrap/react-icon imports from this launcher reduces coupling and avoids pulling feature dependencies through this entry surface. No network, timer, map-render, service, route or icon-resolver behavior was added.
- SECURITY: no HTML string sink, external URL, new dependency, secret, analytics or remote asset host added. The Vitest fix changes only an assertion to match the established non-modal focus ownership contract.
- CURRENT SNAPSHOT: base...head before this progress commit = 1,129 additions / 342 deletions / 17 files. Mandatory 4,000 meaningful-additions gate remains unmet; PR #110 MUST remain draft/open and MUST NOT MERGE.
- TEST / BUILD: fresh exact-head GitHub Actions are required after these commits. Do not infer PASS from earlier runs. Existing full-suite baseline has unrelated legacy failures and Vite dependency-scan JSX-in-.js diagnostics; the exact-base regression gates remain authoritative during staged migration.
- SONRAKİ GÖREV: inspect fresh exact-head Platform Architecture Audit / Webclient Quality / Release QA first and repair only real regressions. If green, integrate `createManagedWindowFocusLifecycle()` into a real managed query/measurement window with integration-level focus-return coverage; then continue a substantive second query-screen strict-TS adoption and 2B↔3B/Measurement interaction/accessibility modernization on this same PR while main remains exact. Merge only after >=4,000 meaningful additions, completed+success exact-head checks, mergeable=true, final responsive/accessibility/interaction review and final main refresh.
