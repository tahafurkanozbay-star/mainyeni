# Kent Rehberi — Geliştirme İlerleme Kaydı

> Full historical progress remains available on `main` at the parent commit. This branch-scoped record summarizes the active Experience continuation without rewriting other teams' canonical history.

## Deep Experience / Whole-Code Modernization — 2026-09-18 01:21 TRT
- TUR / GÖREV: UI/UX + Visual Quality continuation; first real query-window adoption of the strict-TypeScript enterprise Experience foundation.
- BASE MAIN: `172581511c10401d49c3ad4ac98c45f383598df1`; re-verified unchanged immediately after implementation.
- BRANCH / PR: `agent/experience-ui-20260917-2222-1725815`, PR #110 `feat(experience): rebuild typed enterprise UX on current main`.
- VERIFIED BASELINE HEAD: `648502f90f29aa96d1f5e0d209f646e4f37ecde2`; Platform Architecture Audit, Webclient Quality and Release QA all completed successfully on that exact head. The new implementation head is `9cf4b4c815b6f5c3675de0445be842a96a2b3a13`; fresh exact-head Actions had not appeared at the checkpoint, so no PASS is claimed for the new code.
- PR SNAPSHOT: 733 additions / 300 deletions / 13 files after the real-screen adoption. Mandatory 4,000 meaningful-additions gate remains unmet; PR stays draft/open and MUST NOT MERGE. GitHub temporarily reports mergeable=false on the just-updated head; next turn must distinguish transient mergeability calculation from a real conflict before adding more work.
- ÖNEMLİ ÖZELLİKLER: migrated `ParklarQueryWindow` from legacy JS/ad-hoc result cards to strict TSX using shared `ExperienceInput`, `ExperienceStatus`, `ExperienceToolbar` and generic `ExperienceDataTable`; preserved existing ArcGIS clustering, extent restoration, bounded extent history, layer ownership/disposal, route lookup and mobile window minimisation behavior.
- UX / ACCESSIBILITY: park search is now a semantic search form with explicit label and busy state; results expose live status, loading/empty states, keyboard row activation, selected-row state, semantic table caption, toolbar keyboard model and explicit route action labels. Mobile layout collapses the search form, result table uses the existing responsive card reflow, controls keep >=44px touch targets, focus-visible is explicit, reduced-motion spinner suppression and forced-colors borders are retained.
- DİL / MİMARİ: the real screen is now TypeScript rather than adding a parallel demo component. React 19 + Vite 8 + TypeScript 7 remain the chosen stable stack; no framework replacement or new dependency was justified. The legacy `.js` Parklar query implementation was removed after the `.tsx` replacement was committed.
- NETWORK DEĞİŞİKLİKLERİ: no new application endpoint, WMS/WFS/WMTS, CDN, analytics, remote font, secret or dependency. Existing route action still opens the pre-existing Google Maps destination in a new `noopener,noreferrer` window; no new transport path was introduced.
- GÜVENLİK / İKON: no unsafe HTML or client secret introduced. Existing park picture-marker asset remains unchanged; shared deterministic icon authority was not forked or duplicated.
- PERFORMANS ETKİSİ: result rendering uses the shared table instead of nested ad-hoc interactive cards; existing request-version stale-result guard, bounded 30-entry extent history and layer owner disposal remain. No polling or unbounded timer/collection added.
- ÇÖZÜLEN HATALAR / RİSKLER: search no longer auto-fires every time the managed window is shown; users can edit the park name and explicitly submit. Empty, busy and map-not-ready states are now visible/announced instead of silently rendering an empty card area. Nested card-level click handling around result action controls is removed in favor of row keyboard activation plus explicit action buttons.
- TESTLER / BUILD / CI: baseline exact-head CI is green; new head requires fresh strict lint, TypeScript, Vitest and production build via Webclient Quality plus Architecture/Release checks. If CI reports TypeScript contract issues in the legacy window-manager/ArcGIS boundary, fix them on this same PR before further adoption and run a second verification.
- KALAN SORUNLAR / SONRAKİ GÖREV: first inspect exact-head `9cf4b4...` CI and PR mergeability; fix any real compiler/test/build/conflict issue. If main remains exact and branch is mergeable, continue the same PR with another high-traffic query window, managed-window focus restore/lifecycle, Measurement and 2B↔3B interaction polish, plus focused regression tests. Continue until >=4,000 meaningful additions; do not pad line count and do not merge early.
