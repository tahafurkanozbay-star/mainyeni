name: Admin API Lock Refresh (temporary)

on:
  push:
    branches:
      - 'agent/admin-shell-split-20260921-1605-cf1f748'
    paths:
      - 'Webclient.admin/package.json'
      - '.github/workflows/admin-api-lock-refresh.yml'

permissions:
  contents: write

concurrency:
  group: admin-api-lock-refresh
  cancel-in-progress: false

jobs:
  refresh-lock:
    runs-on: ubuntu-24.04
    timeout-minutes: 10
    defaults:
      run:
        working-directory: Webclient.admin
    steps:
      - name: Checkout branch
        uses: actions/checkout@v7
        with:
          ref: agent/admin-shell-split-20260921-1605-cf1f748
          fetch-depth: 1

      - name: Setup Node 24
        uses: actions/setup-node@v7
        with:
          node-version: 24

      - name: Generate lockfile without lifecycle scripts
        run: npm install --package-lock-only --ignore-scripts --no-audit --no-fund

      - name: Commit lockfile and remove temporary workflow
        working-directory: .
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add Webclient.admin/package-lock.json
          git rm .github/workflows/admin-api-lock-refresh.yml
          git commit -m "chore(admin-api): refresh axios-free lockfile"
          git push origin HEAD:agent/admin-shell-split-20260921-1605-cf1f748
