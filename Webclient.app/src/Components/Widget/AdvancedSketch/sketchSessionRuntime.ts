import type { JsonObject, SketchDiagnosticEvent, SketchDiagnosticSink, SketchDocument, SketchGraphicSnapshot, SketchSessionBudget, SketchSessionRuntime, SketchSessionSnapshot, SketchStyleState, SketchTool, SketchViewAdapter } from './sketchContracts';
import { isDrawingTool, isSketchTool } from './sketchContracts';
import { createSketchDocument } from './sketchDocumentRuntime';
import { createSketchHistoryRuntime } from './sketchHistoryRuntime';
import { createSketchMetricsRuntime } from './sketchMetricsRuntime';
import { DEFAULT_SKETCH_STYLE, normalizeSketchStyle } from './sketchStyleRuntime';
const DEFAULT_BUDGET: SketchSessionBudget = Object.freeze({ maxGraphics: 2_000, maxHistoryEntries: 64, maxQueuedOperations: 32, operationTimeoutMs: 15_000 });
const normalizeBudget = (input: Partial<SketchSessionBudget> = {}): SketchSessionBudget => Object.freeze({ maxGraphics: Math.min(20_000, Math.max(1, Math.floor(input.maxGraphics ?? DEFAULT_BUDGET.maxGraphics))), maxHistoryEntries: Math.min(512, Math.max(2, Math.floor(input.maxHistoryEntries ?? DEFAULT_BUDGET.maxHistoryEntries))), maxQueuedOperations: Math.min(256, Math.max(1, Math.floor(input.maxQueuedOperations ?? DEFAULT_BUDGET.maxQueuedOperations))), operationTimeoutMs: Math.min(120_000, Math.max(1_000, Math.floor(input.operationTimeoutMs ?? DEFAULT_BUDGET.operationTimeoutMs))) });
const noopSink: SketchDiagnosticSink = Object.freeze({ emit: () => undefined });
interface Operation<T> { readonly id: string; readonly run: () => Promise<T>; readonly resolve: (value: T) => void; readonly reject: (reason: unknown) => void }
const normalizeError = (error: unknown): Error => error instanceof Error ? error : new Error(String(error ?? 'Unknown sketch error'));
const cloneGraphics = (values: readonly SketchGraphicSnapshot[]): readonly SketchGraphicSnapshot[] => Object.freeze([...values]);
export const createSketchSessionRuntime = (adapter: SketchViewAdapter, options: { readonly budget?: Partial<SketchSessionBudget>; readonly initialStyle?: SketchStyleState; readonly diagnosticSink?: SketchDiagnosticSink; readonly now?: () => number } = {}): SketchSessionRuntime => {
 const budget=normalizeBudget(options.budget); const history=createSketchHistoryRuntime([], {maxEntries:budget.maxHistoryEntries,...(options.now?{now:options.now}:{})}); const metrics=createSketchMetricsRuntime(); const sink=options.diagnosticSink??noopSink; const now=options.now??Date.now;
 let disposed=false,state:SketchSessionSnapshot['state']='ready',selectedTool:SketchTool='move',graphics:readonly SketchGraphicSnapshot[]=Object.freeze([]),style=normalizeSketchStyle(options.initialStyle??DEFAULT_SKETCH_STYLE),activeOperation:string|null=null,errors=0,lastError:string|null=null,operationSequence=0,draining=false; const queue:Operation<unknown>[]=[];
 const emit=(type:SketchDiagnosticEvent['type'],detail:Readonly<Record<string,string|number|boolean|null>>=Object.freeze({})):void=>{try{sink.emit(Object.freeze({type,timestamp:now(),detail}));}catch(error){globalThis.reportError?.(error);}};
 const snapshot=():SketchSessionSnapshot=>Object.freeze({state,selectedTool,graphicsCount:graphics.length,queuedOperations:queue.length,activeOperation,errors,lastError,history:history.snapshot()});
 const fail=(error:unknown,operation:string):Error=>{const normalized=normalizeError(error);errors+=1;lastError=normalized.message.slice(0,500);state=disposed?'disposed':'error';metrics.recordFailure();emit('operation-failed',{operation,message:lastError});return normalized;};
 const withTimeout=async<T>(operation:string,task:()=>Promise<T>):Promise<T>=>{let timer:ReturnType<typeof setTimeout>|null=null;const timeout=new Promise<never>((_,reject)=>{timer=setTimeout(()=>reject(new Error(`Sketch operation ${operation} exceeded ${budget.operationTimeoutMs}ms.`)),budget.operationTimeoutMs);});try{return await Promise.race([task(),timeout]);}finally{if(timer!==null)clearTimeout(timer);}};
 const drain=async():Promise<void>=>{if(draining||disposed)return;draining=true;try{while(queue.length&&!disposed){const operation=queue.shift();if(!operation)continue;activeOperation=operation.id;state=selectedTool==='move'?'updating':'drawing';try{const value=await withTimeout(operation.id,operation.run);state=selectedTool==='move'?'ready':'drawing';lastError=null;operation.resolve(value);}catch(error){operation.reject(fail(error,operation.id));}finally{activeOperation=null;}}}finally{draining=false;if(!disposed&&state==='updating')state='ready';}};
 const enqueue=<T>(name:string,run:()=>Promise<T>):Promise<T>=>{if(disposed)return Promise.reject(new Error('Sketch session is disposed.'));const admitted=queue.length+(activeOperation===null?0:1);if(admitted>=budget.maxQueuedOperations){emit('operation-rejected',{operation:name,reason:'queue-budget'});return Promise.reject(new RangeError('Sketch operation queue capacity exceeded.'));}return new Promise<T>((resolve,reject)=>{operationSequence+=1;queue.push({id:`${name}-${operationSequence}`,run,resolve:(value)=>resolve(value as T),reject} as Operation<unknown>);void drain();});};
 const persistHistory=(label:string)=>history.record(label,graphics); const replaceGraphics=async(next:readonly SketchGraphicSnapshot[],label:string)=>{if(next.length>budget.maxGraphics)throw new RangeError(`Sketch graphic budget ${budget.maxGraphics} exceeded.`);await adapter.replaceGraphics(next);graphics=cloneGraphics(next);persistHistory(label);};
 const selectTool=async(toolInput:SketchTool):Promise<SketchSessionSnapshot>=>{if(!isSketchTool(toolInput))throw new TypeError('Unknown sketch tool.');if(disposed)throw new Error('Sketch session is disposed.');if(toolInput==='clear'){await enqueue('clear',async()=>{await adapter.cancelCreate();await adapter.clearGraphics();graphics=Object.freeze([]);persistHistory('Clear drawings');metrics.recordClear();emit('graphics-cleared');});selectedTool='move';state='ready';return snapshot();}selectedTool=toolInput;metrics.recordToolSelection(toolInput);if(isDrawingTool(toolInput)){await enqueue('begin-create',async()=>{await adapter.beginCreate(toolInput,style);});state='drawing';}else{await enqueue('cancel-create',async()=>{await adapter.cancelCreate();});state='ready';}emit('tool-selected',{tool:toolInput});return snapshot();};
 const ingestGraphic=async(graphic:SketchGraphicSnapshot,label='Add drawing')=>{await enqueue('ingest-graphic',async()=>{const index=graphics.findIndex(x=>x.id===graphic.id);const next=index>=0?graphics.map((x,i)=>i===index?graphic:x):[...graphics,graphic];if(next.length>budget.maxGraphics)throw new RangeError(`Sketch graphic budget ${budget.maxGraphics} exceeded.`);if(index>=0)await adapter.replaceGraphics(next);else await adapter.addGraphic(graphic);graphics=cloneGraphics(next);persistHistory(label);emit('graphic-added',{id:graphic.id});});return snapshot();};
 const observeGraphic=(graphic:SketchGraphicSnapshot,label='Observe drawing')=>{if(disposed)throw new Error('Sketch session is disposed.');const index=graphics.findIndex(x=>x.id===graphic.id);const next=index>=0?graphics.map((x,i)=>i===index?graphic:x):[...graphics,graphic];if(next.length>budget.maxGraphics)throw new RangeError(`Sketch graphic budget ${budget.maxGraphics} exceeded.`);graphics=cloneGraphics(next);persistHistory(label);emit('graphic-added',{id:graphic.id});return snapshot();};
 const observeGraphics=(observed:readonly SketchGraphicSnapshot[],label='Observe drawing update')=>{if(disposed)throw new Error('Sketch session is disposed.');if(observed.length>budget.maxGraphics)throw new RangeError(`Sketch graphic budget ${budget.maxGraphics} exceeded.`);graphics=cloneGraphics(observed);persistHistory(label);return snapshot();};
 const removeGraphic=async(idInput:string,label='Remove drawing')=>{const id=String(idInput??'').trim();if(!id)throw new TypeError('Graphic id is required.');await enqueue('remove-graphic',async()=>{const next=graphics.filter(g=>g.id!==id);if(next.length===graphics.length)return;await adapter.removeGraphic(id);graphics=cloneGraphics(next);persistHistory(label);emit('graphic-removed',{id});});return snapshot();};
 const clear=async(label='Clear drawings')=>{await enqueue('clear',async()=>{await adapter.cancelCreate();await adapter.clearGraphics();graphics=Object.freeze([]);persistHistory(label);metrics.recordClear();emit('graphics-cleared');});selectedTool='move';state='ready';return snapshot();};
 const undo=async()=>{const previous=history.undo();if(!previous)return snapshot();await enqueue('undo',async()=>{await adapter.cancelCreate();await adapter.replaceGraphics(previous);graphics=cloneGraphics(previous);metrics.recordUndo();emit('history-undo');});selectedTool='move';state='ready';return snapshot();};
 const redo=async()=>{const next=history.redo();if(!next)return snapshot();await enqueue('redo',async()=>{await adapter.cancelCreate();await adapter.replaceGraphics(next);graphics=cloneGraphics(next);metrics.recordRedo();emit('history-redo');});selectedTool='move';state='ready';return snapshot();};
 const importDocument=async(document:SketchDocument)=>{await enqueue('import-document',async()=>{await adapter.cancelCreate();style=normalizeSketchStyle(document.style);await replaceGraphics(document.graphics,'Import sketch document');metrics.recordImport();emit('document-imported',{graphics:document.graphics.length});});selectedTool='move';state='ready';return snapshot();};
 const exportDocument=(title='Çizim')=>{if(disposed)throw new Error('Sketch session is disposed.');metrics.recordExport();emit('document-exported',{graphics:graphics.length});const metadata:JsonObject=Object.freeze({metrics:JSON.stringify(metrics.calculate(graphics))});return createSketchDocument(graphics,style,title,metadata,new Date(now()));};
 const setStyle=(nextStyle:SketchStyleState)=>{if(disposed)throw new Error('Sketch session is disposed.');style=normalizeSketchStyle(nextStyle,style);return snapshot();};
 const dispose=async()=>{if(disposed)return;disposed=true;state='disposed';for(const operation of queue.splice(0))operation.reject(new Error('Sketch session disposed.'));try{await adapter.cancelCreate();}finally{await adapter.destroy();emit('disposed');}};
 return Object.freeze({selectTool,ingestGraphic,observeGraphic,observeGraphics,removeGraphic,clear,undo,redo,importDocument,exportDocument,setStyle,snapshot,dispose});
};
