import { createMapQueryBusiness } from './createMapQueryBusiness';

export const PlanQueryBusiness = createMapQueryBusiness({
  serviceKey: 'PlanQueryUrl',
  searchField: 'adi',
  orderByField: 'adi',
});
