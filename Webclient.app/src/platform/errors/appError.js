// Compatibility adapter during the staged JavaScript -> TypeScript platform migration.
// Existing consumers keep this stable path while the implementation lives in appError.ts.
export * from './appError.ts';
