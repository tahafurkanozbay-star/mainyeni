// Compatibility adapter during the staged JavaScript -> TypeScript platform migration.
// Existing startup code keeps this import path while the monitor implementation is typed.
export * from './performanceMonitor.ts';
