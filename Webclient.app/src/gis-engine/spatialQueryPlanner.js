// Compatibility adapter during the staged JavaScript -> TypeScript GIS migration.
// Existing imports continue to resolve this path while the implementation lives in spatialQueryPlanner.ts.
export * from './spatialQueryPlanner.ts';
